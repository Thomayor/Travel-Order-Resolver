# CamemBERT NER Training Requirements
# Install with: pip install -r requirements_camembert.txt

# Core ML libraries
transformers>=4.30.0
datasets>=2.12.0
torch>=2.0.0
accelerate>=0.20.0

# Metrics and evaluation
evaluate>=0.4.0
seqeval>=1.2.2

# Data processing
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# TensorBoard (for monitoring)
tensorboard>=2.13.0

# Optional: For faster training
# sentencepiece>=0.1.99  (already included in transformers)
